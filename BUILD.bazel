load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "breakout",
    srcs = [
        "main.cpp"
    ],
    deps = [
        "@glfw//:glfw",
        "@glm//:glm",
        "@spdlog//:spdlog",
        "//third_party/glad:glad"
    ]
)