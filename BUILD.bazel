load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "breakout",
    srcs = [
        "main.cpp",
    ],
    copts = ["-std=c++23"],
    deps = [
        "//third_party/glad",
        "//third_party/imgui",
        "@glfw",
        "@glm",
        "@spdlog",
    ],
)
